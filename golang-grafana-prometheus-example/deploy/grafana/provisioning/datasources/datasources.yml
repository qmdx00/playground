apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090 # Prometheus service name in Docker network
    access: proxy
    isDefault: true
    editable: true
